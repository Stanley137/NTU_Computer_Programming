#include <stdio.h>
int main(){
    int m;
    scanf("%d", &m);
    char *book[100], *author[100], *final_author[100];
    for(int i = 0;i<m;i++){
        scanf("%s%s",author[i], book[i]);
    }
}